# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0

# HTTP Requests (for Groq and Google APIs)
httpx==0.27.0
requests==2.32.0

# Configuration
pyyaml==6.0.2
python-dotenv==1.0.0

# Machine Learning (for complexity classifier)
scikit-learn==1.5.0
joblib==1.4.0
numpy==1.26.0
pandas==2.2.0

# Token Counting
tiktoken==0.7.0

# Dashboard
streamlit==1.38.0
plotly==5.24.0


# ── LLM Providers ──────────────────────────────────────────────────────────────
# Groq API - Fast inference with free tier
groq==0.9.0

# Google Generative AI - Gemini models
google-generativeai==0.7.0

#Testing 
pytest==7.4.3

# Database (SQLite comes with Python, no install needed)
# No extra packages needed

# Optional: For local model testing
# ollama installed separately via curl/installer, not pip